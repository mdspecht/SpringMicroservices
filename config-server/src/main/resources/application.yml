server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: c:\Users\mds_s\Documents\src\pessoal\SpringMicroservices\config

